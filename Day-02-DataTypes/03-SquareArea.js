/*
 * Day 2 - Square Area
 * Instructions:
 * - Run this file with Node.js: node 03-SquareArea.js
 * - This program calculates the area of a square
 * - Try different values for the side length
 * - Hint: Area of a square = side × side
 */

function squareArea(a) {
    let area = a * a;
    console.log(area);
}
squareArea(5);
